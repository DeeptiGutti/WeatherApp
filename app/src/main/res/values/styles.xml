<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="ButtonStyle" parent="Widget.AppCompat.Button">
        <item name="backgroundTint">#C36241</item> <!-- Set the background color to #357EC7 -->
        <item name="android:textColor">@android:color/white</item> <!-- Adjust the text color as desired -->
        <item name="android:textSize">14sp</item> <!-- Adjust the text size as desired -->
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_weight">1</item>
        <item name="android:gravity">center</item>
        <item name="android:padding">8dp</item> <!-- Adjust the padding as desired -->
    </style>
</resources>